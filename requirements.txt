# Runtime dependencies (CPU). For the GPU path install the optional extra:
#   pip install -e ".[gpu]"    # adds cupy-cuda12x on a CUDA 12 node
numpy>=1.24
pandas>=2.0
xarray>=2023.1.0
netCDF4>=1.6
scikit-learn>=1.3
